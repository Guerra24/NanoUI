subprojects {
	project.ext.globalVersion = "0.1.0"
	
	repositories {
		mavenCentral()
		maven { url "https://oss.sonatype.org/content/repositories/releases/" }
	}
}

task wrapper(type: Wrapper) { gradleVersion = '4.7' }
